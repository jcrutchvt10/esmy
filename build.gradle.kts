// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    // Android Application plugin is applied in the app module.
    // Kotlin Android plugin is applied in the app module.
    // No plugins applied here.
}

repositories {
    google()
    mavenCentral()
}
